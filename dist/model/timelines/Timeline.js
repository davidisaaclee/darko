// Generated by CoffeeScript 1.9.2
(function() {
  var Model, Timeline, _,
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  _ = require('lodash');

  Model = require('../Model');


  /*
   * Represents a timeline model.
   * These timeline models are referenced by entities; so they only hold
   *   information applicable to any attached entity - such as triggers,
   *   mappings, and timeline length.
  Timeline ::=
     * Progress along the timeline is scaled by its `length`.
     * If timeline A has 2x the length of timeline B, it should take twice as long
     *   to progress along A as to progress along B.
    length: Number
   */

  Timeline = (function(superClass) {
    extend(Timeline, superClass);

    Timeline.type = 'Timeline';

    function Timeline(type, length, shouldLoop1) {
      this.type = type;
      this.length = length != null ? length : 1;
      this.shouldLoop = shouldLoop1 != null ? shouldLoop1 : false;
    }

    Timeline.getLength = function(timeline) {
      return timeline.length;
    };

    Timeline.getShouldLoop = function(timeline) {
      return timeline.shouldLoop;
    };

    Timeline.setLoop = function(timeline, shouldLoop) {
      return _.assign({}, timeline, {
        shouldLoop: shouldLoop
      });
    };

    Timeline.reducer = function(timeline) {
      return function(data, changes) {
        return console.warn('Timeline should override Timeline.reducer: ', timeline);
      };
    };

    Timeline.progress = function(timeline, progress, data) {
      return console.warn('Timeline should override Timeline.progress: ', timeline);
    };

    Timeline.typeOf = function(timeline) {
      return timeline.type;
    };

    return Timeline;

  })(Model);

  module.exports = Timeline;

}).call(this);

//# sourceMappingURL=Timeline.js.map
